import { SlideOutDirDirective } from './slide-out-dir.directive';

describe('SlideOutDirDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideOutDirDirective();
    expect(directive).toBeTruthy();
  });
});
